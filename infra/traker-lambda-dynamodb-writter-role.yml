Resources:
  TrakerLambdaDynamodbWritterRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: ${self:service}-d-${self:provider.stage}ynamodb-writter-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - 'sts:AssumeRole'
      Policies:
        - PolicyName: ${self:service}-${self:provider.stage}-dynamodb-writter-role-policy-policy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"
              - Effect: Allow
                Action:
                  - dynamodb:PutItem
                Resource:
                  Fn::GetAtt: [TrackingsTable, Arn]
              - Effect: Allow
                Action:
                  - dynamodb:PutItem
                Resource:
                  Fn::GetAtt: [OccurrencesTable, Arn]
              - Effect: Allow
                Action:
                  - dynamodb:DeleteItem
                Resource:
                  Fn::GetAtt: [TrackingsTable, Arn]
              - Effect: Allow
                Action:
                  - dynamodb:DeleteItem
                Resource:
                  Fn::GetAtt: [OccurrencesTable, Arn]
